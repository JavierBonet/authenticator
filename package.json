{
  "name": "authenticator",
  "version": "1.0.0",
  "description": "NodeJS authentication and authorization logic with a simple frontend",
  "main": "index.js",
  "repository": "https://github.com/JavierBonet/authenticator.git",
  "author": "Javier Bonet <javbonet@gmail.com>",
  "license": "MIT",
  "scripts": {
    "install-root": "echo \"Installing root dependencies...\"; yarn",
    "install-backend": "echo \"Installing backend dependencies...\"; cd backend; yarn",
    "install-frontend": "echo \"Installing frontend dependencies...\"; cd frontend; yarn",
    "install-all": "yarn install-root; yarn install-backend; yarn install-frontend",
    "start-backend": "cd backend; yarn start",
    "build-backend": "cd backend; yarn build",
    "build-frontend": "cd frontend; yarn build",
    "start-frontend": "cd frontend; yarn start",
    "start": "npm-run-all --parallel start-backend start-frontend"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.6"
  }
}
